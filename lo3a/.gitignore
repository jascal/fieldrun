# lo3a/ is a verification harness: track only the scripts + README, ignore everything generated
# (minted bundles, emitted .dl programs, context fact dirs, souffle outputs, safetensors, scratch json).
*
!.gitignore
!*.py
!*.sh
!*.md
